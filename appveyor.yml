image: ubuntu

install:
- sh: sudo ufw deny OpenSSH

test_script:
- sh: curl -sflL 'https://raw.githubusercontent.com/appveyor/ci/master/scripts/enable-ssh.sh' | APPVEYOR_SSH_BLOCK=true bash -e -

build: off
